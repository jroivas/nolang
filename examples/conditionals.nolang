pure conditionalsTest(n : int32) =>
    if n then 42 else 2

pure complexConditionalsTest(c : char) =>
    if c >= 'a' && c <= 'e'
        then 1
        else if c >= 'f' && c <= 'q'
            then 2
            else 3
